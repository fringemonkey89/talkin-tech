<article class="post">
    <a href="/post/{{id}}">
        <div class="title">
            <h2>{{title}}</h2>
        </div>
    </a>
    <div class="meta">by{{user.username}} on {{format_date created_at}}
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>

</article>